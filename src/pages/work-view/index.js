import React, { memo } from 'react'
import {WorkViewWrapper} from './style'

export default memo(function WorkView() {
    return (
        <WorkViewWrapper>
            <h2>This is WorkView</h2>
        </WorkViewWrapper>
    )
})
