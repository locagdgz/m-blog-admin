import React, { memo } from 'react'

import {Button} from 'antd'

export default memo(function Login() {
    return (
        <div>
            <Button>
                I'm Button

            </Button>
        </div>
    )
})
